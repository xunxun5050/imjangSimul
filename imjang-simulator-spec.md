# 임장 시뮬레이터 기능 명세서

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.2 |
| 대상 산출물 | `imjang-simulator.html` · `pilot/collect_listings.py` · `pilot/enrich_kapt.py` |
| 작성 기준일 | 2026-09-16 |
| 대상 독자 | 구현·수정을 담당할 개발자 |

**v1.2 변경 요약** (v1.1 실거래 연동 이후 반영)

- 게임 시작 직후 **인트로 몽타주**(캐릭터가 지도 위를 이동하는 연출) 추가 — `phase: "intro"` (§4.2)
- 우선순위 7종 → **10종** (`parking`/`big`/`value` 추가), 직장 7곳 → **14곳**
- **임장 갈 지역 다중 선택** — 실거래 모드에서는 절차적 36개 지역 대신 불러온 `listings.json`의 법정동이 그대로 선택 대상이 된다 (§5.6)
- 생애최초 LTV를 "+10%p"에서 **80% 단일 적용**으로, DSR 계산에 스트레스 금리 가산으로 현실화 (§5.3)
- **매물 소스가 절차적 생성 / 실거래 풀 두 모드로 분리**됨 (§3.7). `listings.json` 스키마와 수집·보강 파이프라인 추가 (§3.8)
- 매물이 사용자 예산을 넘어 나오던 "못 살 집" 노출을 없애고, **예산 재시도 로직**으로 항상 대출 한도 안의 매물만 생성 (§5.2.1) — 실거래 모드도 동일하게 적용
- 임장 동선(§4.3) 로그 추가

---

## 1. 개요

### 1.1 한 줄 정의
플레이어가 캐릭터의 소득·현금·선호를 설정하면, 정해진 기간 동안 수도권 아파트 매물을 주 단위로 보러 다니다가 한 채를 계약하는 1인용 시뮬레이션.

### 1.2 설계 목표
- 매물 가격과 대출 한도가 **실제 수도권 규칙을 단순화한 수식**으로 결정될 것. 랜덤 숫자를 뿌리지 않는다.
- 공개된 실거래 데이터가 있으면 **실제 단지·실제 가격**으로 돌아갈 것. 없으면 절차적 생성으로 폴백한다.
- 플레이어가 매 턴 개입하되, **통제 불가능한 변수**(호가 인상, 매물 소진, 규제 변경)가 결과를 흔들 것.
- 실패 엔딩(미계약)이 존재할 것. 반드시 집을 사는 게임이 아니다.

### 1.3 범위 밖
전세·월세·분양·경매·다주택·임대사업자. v1은 **매매, 실거주 1채**만 다룬다.

### 1.4 용어

| 용어 | 정의 |
|---|---|
| 주(week) | 게임의 최소 시간 단위. 1주 = 매물 1건 임장 또는 휴식 1회 |
| 매물(Listing) | 임장 대상 1건. 지역·평형·연식·층·플래그·가격을 가짐 |
| 풀(POOL) | `listings.json`에서 로드한 실거래 기반 매물 후보 집합. 없으면 절차적 생성으로 동작 |
| heat | 매물의 경쟁 과열도(0.05~0.95). 계약·협상 성공률을 결정 |
| marketIdx | 시장 가격 지수. 신규 생성 매물 가격에 곱해짐 |
| marketMood | 시장 심리(0.6~1.5). heat·성공률·1년 후 시세에 영향 |
| 체력(stamina) | 0~100. 낮으면 임장 리포트 단점이 가려지고 성공률이 떨어짐 |
| 만원 | 모든 금액의 내부 단위. 1억 = 10000 |

---

## 2. 시스템 구성

- 게임 본체는 단일 HTML 파일. 빌드 도구·프레임워크·번들러 없음.
- 외부 의존성은 Google Fonts(`Black Han Sans`, `IBM Plex Sans KR`) 뿐. 폰트 로드 실패 시 시스템 한글 폰트로 폴백한다.
- 전역 상태 객체 `S` 하나(+ 실거래 풀은 `S` 바깥의 별도 전역 `POOL`) + `render()` 전체 재그리기. 가상 DOM 없음.
- **영속 저장 없음.** 새로고침하면 초기화된다. (localStorage 사용 불가 환경) `POOL`은 예외적으로 게임을 다시 시작해도 유지된다 — 새로고침 전까지는 불러온 데이터를 다시 올릴 필요가 없다.
- 데이터 수집은 별도 오프라인 스크립트(`pilot/`). 게임은 결과물(`listings.json`)만 읽는다.

```
renderSetup() ──(listings.json 로드, 선택)──▶ loadPool()
      │
      └──(임장 시작)──▶ newGame(cfg) ──▶ renderIntro() ──(몽타주 종료)──▶ startTour()
                                                                              │
                                                                              ▼
                                                                    ┌─ nextWeek() ─┐
                                                                    │              │
                                                              renderTour()  ◀──────┘
                                                                    │
                                ┌───────────────────┬───────────────────┐
                            tryContract()      negotiate()          addWatch()
                            revisit()          rest()               pass
                                │
                            성공 or 기간 만료
                                ▼
    renderEnd()
```

---

## 3. 데이터 모델

### 3.1 Region (36건, 상수)

절차적 모드의 지역 소스다. 실거래 모드에서는 `listings.json`의 `dongMeta`가 `regionUniverse()`를 통해 이 자리를 대체한다 (§3.7).

```js
{ n:"강남구 대치", z:"서울", p:11200, x:7, y:-6, t:1.00,
  edu:5, inf:5, env:3, fut:5 }
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `n` | string | 표시명 (구 + 대표 동) |
| `z` | `"서울"` \| `"경기"` \| `"인천"` | LTV 적용 구분 |
| `p` | number | 34평 기준 평당가 (만원/평). 실거래 모드는 로드 시 해당 법정동 평균 평당가로 역산(표시용) |
| `x`, `y` | number | 서울시청 기준 좌표 (km). 동쪽 +x, 북쪽 +y |
| `t` | number | 교통 불편 계수 (1.00~1.30). 클수록 통근 시간 증가 |
| `edu`/`inf`/`env`/`fut` | 1~5 | 학군 / 인프라 / 환경 / 미래가치 |

### 3.2 Office (14건, 상수)
`{ n:"강남역", x:5, y:-7 }` — 강남역, 광화문, 여의도, 판교, 구로디지털단지, 상암DMC, 마곡, 잠실, 가산디지털단지, 성수, 분당 정자, 수원 삼성, 화성 동탄, 인천 송도.

### 3.3 Size (4건, 상수)

| 평형 | 전용 | `mul` (평당가 보정) | 방 |
|---|---|---|---|
| 25평 | 59㎡ | 1.12 | 방2 |
| 30평 | 74㎡ | 1.05 | 방3 |
| 34평 | 84㎡ | 1.00 | 방3 |
| 45평 | 114㎡ | 0.95 | 방4 |

소형일수록 평당가가 비싸다는 실제 경향을 반영한다.

### 3.4 Priority (10종)
`commute`(출퇴근), `edu`(학군), `fresh`(신축), `size`(넓은 집), `fut`(미래가치), `env`(조용한 환경), `inf`(상권·인프라), `parking`(주차), `big`(대단지), `value`(가성비). 플레이어는 **최대 2개** 선택.

- `commute`/`edu`/`fut`/`env`/`inf`는 지역 단위 속성이라 `regionFit`(§5.6) 가중치에도 반영되어 어떤 지역이 매물로 뜨는지에 영향을 준다.
- `fresh`/`size`/`parking`/`big`는 매물 생성 시 해당 속성이 유리하게 나올 확률·범위를 높인다(§5.2, `makeListing`).
- `value`(가성비)는 생성에는 영향을 주지 않고, 매물가가 그 지역·평형에서 살 수 있는 최대가 대비 얼마나 저렴한지로 만족도(§5.9)에만 반영된다.

### 3.5 Listing

```js
{ id, r /*Region*/, size /*Size*/, age, floor, tot,
  flags, price, basePrice, heat,
  name, stationMin, units, commute, seenWeek,
  // 실거래 모드에서만 채워짐
  stationName, heatName, totalFloorsEst, real:{n, median, lastYm, kapt} }
```

`real` 블록이 있으면(= 실거래 매물이면) 매물 슬립에 "최근 N건 실거래 중앙값 · 최종 거래 YYYY년 M월 · K-apt 단지정보 반영" 근거 줄이 붙는다. `totalFloorsEst`가 참이면 층 표기에 `*`가 붙는다(§3.8, `enrich_kapt.py`가 못 채운 값이라는 뜻).

**flags** — 확률은 절차적 모드 기준. 실거래 모드는 아래처럼 실데이터가 있으면 그걸 우선한다.

| 키 | 의미 | 발생 확률 / 실거래 모드 |
|---|---|---|
| `station` | 역세권 | 서울 0.62 / 그 외 0.45 · 실거래: `subwayWalkMin ≤ 10`(없으면 0.55) |
| `big` | 대단지 | 0.45 (우선순위에 `big` 선택 시 0.75) · 실거래: `units ≥ 800`(우선순위에 `big` 선택 시, 미달이어도 0.3 확률로 보정) |
| `school` | 초품아 | 0.40 |
| `noise` | 도로 소음 | 0.22 |
| `hill` | 언덕 | 서울 0.28 / 그 외 0.12 |
| `south` | 남향·개방감 | 0.60 |
| `sun` | 일조 불량 | 0.25 |
| `leak` | 누수 흔적 | 연식>15일 때 0.25 |
| `renov` | 올수리 | 연식>10일 때 0.35 |
| `recon` | 재건축 연한 | 연식≥29일 때 0.50 |
| `tenant` | 세입자 거주 | 0.30 |
| `parking` | 세대당 주차 대수 | 연식≤12: 1.2~1.8 / 그 외 0.5~1.1 (우선순위에 `parking` 선택 시 +0.2~0.5 가산) · 실거래: `parkingPerUnit`이 있으면 그 값 기준(우선순위 선택 시 +0.1~0.3 가산) |

향·수리상태·누수·세입자·소음·일조는 실거래 모드에서도 **그대로 랜덤**이다. 임장을 가야만 알 수 있는 정보는 어떤 API에도 없고, 그게 없다는 사실이 이 게임의 전제다.

연식은 `0~34년` 균등. 단, 우선순위에 `fresh`가 있으면 50% 확률로 `0~9년`으로 제한한다.

### 3.6 GameState (`S`)

| 필드 | 초기값 | 설명 |
|---|---|---|
| `name`/`age`/`house` | 입력 | 캐릭터 정보 |
| `income` | 입력 | 세전 연소득 (만원) |
| `cash` | 입력 | 보유 현금 (만원) |
| `debtAnnual` | 입력 | 기존 대출 연 상환액 |
| `office` | 입력 | 직장. **이벤트로 변경될 수 있음** |
| `prio` | 입력 | 우선순위 키 배열 (1~2개) |
| `regions` | 입력 | 임장 갈 지역 이름 배열(`REGIONS[].n`). 1개 이상. 매물 생성·인트로 몽타주가 이 목록으로 한정된다(`activeRegions()`, §5.6) |
| `months` / `totalWeeks` | 입력 / `months*4` | 임장 기간 |
| `phase` | `"intro"` | 화면 상태. `"intro"`(시작 몽타주) → `"tour"`(임장 진행) → `"end"`(결과) 순으로만 전이 |
| `week` | 1 | 현재 주차 |
| `rate` | 4.2 | 주담대 금리 (%) |
| `ltvSeoul` / `ltvGyeonggi` | 50 / 70 | LTV (%) |
| `firstHome` | 입력 | 생애최초 여부. LTV 80% 단일 적용(지역 기본 LTV 무시) |
| `loanHardCap` | 60000 | 주담대 절대 한도 (6억) |
| `marketIdx` | 1.0 | 가격 지수 |
| `marketMood` | 1.0 | 시장 심리 |
| `stamina` | 100 | 체력 |
| `current` | null | 이번 주 매물 |
| `watch` | `[]` | 관심 매물 (최대 3, FIFO) |
| `log` | `[]` | 임장 일지 (최신순) |
| `toured` | 0 | 누적 임장 건수 |
| `contract` | null | 계약 결과 |
| `visits` | `[]` | 임장 동선 기록. `{week, r, name, price, py, status}` 배열. 매물을 새로 보거나 재방문할 때마다 항목이 추가되고, 그 매물에 대한 후속 행동(패스/관심 등록/협상/계약 등)은 `status`에 `" · "`로 이어 붙는다 |
| `currentVisit` | null | 이번 주 `visits` 항목에 대한 참조. 행동 버튼이 `status`를 갱신할 때 사용 |

`POOL`은 `S` 바깥의 별도 전역이다. 게임을 다시 시작해도(§4.4 "다시 시작하기") 로드한 데이터는 유지된다 — 새로고침해야 사라진다.

### 3.7 매물 소스 — 두 가지 모드

`makeListing()`이 전역 `POOL`의 유무로 분기한다. 게임 로직의 나머지(자금 계산, 이벤트, 예산 재시도, 만족도, 엔딩)는 **두 모드에서 완전히 동일**하다.

| | 절차적 모드 (POOL 없음) | 실거래 모드 (POOL 있음) |
|---|---|---|
| 지역 | 상수 `REGIONS` 36건 | `listings.json`의 `dongMeta` |
| 지역 선택 UI | SETUP "임장 갈 지역" 36개 칩 | 같은 UI, `listings.json`을 불러오면 그 법정동 목록으로 자동 교체 |
| 단지명 | 음절 조합 생성 (`NAME_A`+`NAME_B`) | 실제 단지명 (`complexes[].apt`) |
| 가격 | 평당가 × 계수 곱 (§5.2) | 실거래 중앙값 × 호가 프리미엄 (§5.2) |
| 층 | `1 ~ 총층` 균등 | 실거래 관측 층에서 추출 |
| 연식 | 0~34년 균등 | 사용승인일 또는 건축년도 |
| 세대수·주차·역세권 | 확률 롤 | K-apt 실데이터가 있으면 그걸 사용 (§3.5) |
| 향·수리·누수·세입자·소음 | 확률 롤 | **확률 롤 (동일)** |
| 예산 재시도(§5.2.1) | 적용 | **동일하게 적용** |

`regionUniverse()`가 두 모드의 지역 집합을 통일해서 돌려주고(`POOL`이 있으면 `Object.values(POOL.regions)`, 없으면 `REGIONS`), `activeRegions()`는 그중 플레이어가 SETUP에서 고른 것만 추린다. 인트로 몽타주(§4.2)의 지도·방문 지점도 같은 함수를 쓴다.

### 3.8 데이터 파이프라인

```
국토부 아파트 매매 실거래가 API
        │  pilot/collect_listings.py  (수집 → 해제거래 제외 → 단지·평형 그룹핑 → 중앙값)
        ▼
    listings.json
        │  pilot/enrich_kapt.py  (K-apt 단지 목록 → 단지명 매칭 → 기본/상세정보 병합, 선택)
        ▼
    listings.json (보강)
        │  SETUP 화면 "매물 데이터"에서 파일 선택, 또는 <script id="listings-data"> 인라인
        ▼
      게임
```

#### 3.8.1 수집 — `pilot/collect_listings.py`

| 항목 | 내용 |
|---|---|
| 소스 | 국토교통부 아파트 매매 실거래가 상세 자료 |
| 조회 키 | 법정동코드 앞 5자리 + 계약년월 6자리 |
| 응답 태그 | 한글/영문 두 벌을 모두 시도 (`ALIAS` 맵) |
| 제외 | `해제여부 = O` (취소 거래). 안 거르면 신고가가 오염된다 |
| 그룹핑 | `(단지명, 법정동, round(전용면적))` |
| 대표값 | 거래금액 **중앙값**. 평균은 신고가 한 건에 끌려간다 |
| 최소 표본 | 평형당 3건 (`--min-deals`) |
| 트래픽 | 구 1개 × 12개월 = 12콜. 개발계정 일 10,000건으로 충분 |

`DONG_META`는 법정동별 `x, y, t, edu, inf, env, fut`를 손으로 채운 표다. 절차적 모드의 `REGIONS` 레코드를 그대로 대체하므로, 새 구를 추가할 때 유일하게 사람이 판단해야 하는 부분이다. `pilot/README.md` §5 참고.

#### 3.8.2 보강 — `pilot/enrich_kapt.py` (선택)

| 채우는 값 | 출처 |
|---|---|
| `units` 세대수, `dongCnt` 동수 | 기본정보 `kaptdaCnt`, `kaptDongCnt` |
| `buildYear` | 기본정보 `kaptUsedate` 앞 4자리 (실거래 건축년도보다 정확) |
| `parkingPerUnit` | 상세정보 `(kaptdPcnt + kaptdPcntu) / units` |
| `subwayStation`, `subwayWalkMin` | 상세정보 `subwayStation`, `kaptdWtimesub` |
| `heatName` | 기본정보 `codeHeatNm` |
| `totalFloors` | **K-apt에 없음.** 실거래 관측 최대층 + 2로 추정하고 `totalFloorsEstimated: true` |

매칭은 단지명 정규화(괄호·공백·`아파트` 접미 제거) 후 완전일치 → `difflib` 퍼지매칭(컷오프 0.82) 순이다. `matchScore`를 함께 저장하니 1.0 미만인 건은 눈으로 확인할 것.

두 서비스 모두 엔드포인트 버전이 여러 번 바뀐 이력이 있어, 스크립트가 후보 경로를 순서대로 찔러보고 통한 조합을 캐시한다. 전부 실패하면 포털 활용가이드에서 현재 경로를 확인해 `BASES`/`LIST_BASES`/`INFO_BASES` 상수를 고친다.

#### 3.8.3 `listings.json` 스키마

```jsonc
{
  "meta": {
    "source": "molit-rtms-apt-trade-dev",
    "lawdCd": "11350", "gu": "노원구", "zone": "서울",
    "months": 12, "minDeals": 3,
    "askPremium": 1.05,              // 실거래 중앙값 → 호가 환산 계수
    "generatedAt": "2026-03-01",
    "kaptEnrichedAt": "2026-03-01", "kaptMatched": 168
  },
  "dongMeta": {
    "중계동": { "x": 6, "y": 8, "t": 1.10, "edu": 5, "inf": 3, "env": 4, "fut": 3 }
  },
  "complexes": [{
    "id": "11350-중계동-○○마을1단지",
    "apt": "○○마을1단지", "dong": "중계동",
    "buildYear": 1994,
    "units": 1120, "dongCnt": 14, "totalFloors": 17, "totalFloorsEstimated": true,
    "parkingPerUnit": 0.83,
    "subwayStation": "중계역", "subwayWalkMin": 10, "heatName": "지역난방",
    "kaptCode": "A10351234", "matchScore": 0.94,
    "sizes": [{
      "m2": 84.9, "py": 25.7, "n": 8,
      "median": 98000, "min": 92000, "max": 105000,   // 만원
      "floors": [2,3,5,7,9,11,14,15],
      "lastYm": "202602"
    }]
  }]
}
```

`dongMeta`에 없는 법정동의 거래는 수집 단계에서 조용히 버려진다. `units`/`dongCnt`/`totalFloors`/`parkingPerUnit`/`subwayStation`/`heatName`/`kaptCode`/`matchScore`는 `enrich_kapt.py`가 채우는 값이라, 보강 전에는 없어도(`undefined`) 정상이며 그 경우 `makeListing`이 랜덤값으로 메운다(§3.5).

---

## 4. 화면 명세

### 4.1 SETUP — 캐릭터 설정

**매물 데이터 블록** (최상단, 캐릭터 설정 카드보다 위)

- 상태 문구: 미로드 시 "지금은 가상 매물 생성 모드입니다", 로드 시 `{구} 실거래 기반 · 단지 N개 / 평형 M종 · 기준 YYYY-MM-DD`
- `meta.source === "SAMPLE-SYNTHETIC"`이면 빨간색 "합성 샘플입니다" 경고를 덧붙인다
- `<input type="file" accept="application/json,.json">` — `FileReader`로 읽어 `loadPool()`. 성공하면 SETUP 화면 전체를 다시 그려 "임장 갈 지역" 칩이 새 `dongMeta` 기준으로 바뀐다(그 사이 입력한 다른 필드는 초기화된다)
- 로드 실패 시 `alert`로 사유를 띄우고 절차적 모드를 유지한다
- 배포 시에는 `<script id="listings-data" type="application/json">`에 JSON을 인라인해두면 부팅 시 자동 로드된다

**캐릭터 설정**

| 입력 | 컨트롤 | 기본값 | 검증 |
|---|---|---|---|
| 이름 | text (10자) | 김종국 | 공백이면 "나" |
| 나이 | number | 36 | 22~65 |
| 세전 연소득 | number (만원) | 7500 | ≥1500 |
| 보유 현금 | number (만원) | 30000 | ≥0 |
| 기존 대출 연 상환액 | number (만원) | 0 | ≥0 |
| 직장 | select | 강남역 | — |
| 가구 형태 | chip 단일 | 신혼부부 | — |
| 생애최초 | chip 단일 | 예 | — |
| 우선순위 | chip 다중 | 없음 | **1개 이상 필수, 최대 2개** |
| 임장 갈 지역 | chip 다중(절차적 36개 또는 로드된 `dongMeta` 개수, 서울/경기/인천 구획 — 지역이 없는 구획은 표시 안 함) + 전체 선택·전체 해제 버튼 | 전체 해제(직접 골라야 함) | **1개 이상 필수** |
| 임장 기간 | chip 단일 | 6개월 | 1/2/3/4/6/9/12개월 |

- 입력 변경 시마다 하단에 **실시간 예산 미리보기**를 갱신한다: "대출 최대 X, 살 수 있는 집은 대략 Y까지".
- 우선순위 또는 지역을 하나도 고르지 않고 시작을 누르면 `alert`가 아닌 화면 내 안내 문구(빨간 텍스트)로 표시하고 차단한다. 팝업 차단 환경에서도 항상 눈에 보여야 한다. 우선순위 검증이 먼저이고, 그다음 지역을 검증한다.

### 4.2 INTRO — 시작 몽타주

`임장 시작` 클릭 직후, 첫 매물을 보여주기 전에 재생되는 짧은 연출.

- 지도: `activeRegions()`(+ `OFFICES`)의 `x`,`y`를 정규화해 그린 점 지도. 지도 배율(축척)은 항상 수도권 전체 기준으로 고정하고, 점은 `S.regions`로 고른 지역만 찍는다. 직장 위치를 검정 점으로 표시한다.
- 캐릭터 마커: 갈색 머리·살구색 얼굴·크림색 셔츠(파랑 브이넥)·파랑 바지로 이루어진 작은 사람 캐릭터(단색 아이콘이 아니라 여러 색을 쓴 미니 아바타). 이동할 때마다 마커 아래 링 모양 펄스가 한 번 퍼졌다 사라진다.
- 동선: `regionFit` 가중 추첨으로 선택된 지역 중 서로 다른 곳 최대 7곳을 뽑아(선택한 지역이 7개 미만이면 그 수만큼), 직장 → 지역1 → 지역2 → … 순서로 마커가 약 230ms 간격으로 이동한다. 지나간 경로는 점선 폴리라인으로 누적된다.
- 라벨: `N/7 · 지역명 임장 중...` 형태로 현재 위치를 갱신한다.
- 연출이 끝나면(약 2초) 자동으로 `startTour()`가 호출되어 TOUR 화면 1주차로 넘어간다. 플레이어 입력은 없다(스킵 버튼 없음).
- `prefers-reduced-motion: reduce`이면 몽타주 자체를 생략하고 바로 TOUR 1주차로 진입한다.

### 4.3 TOUR — 임장 진행 (메인 루프)

**상단 고정 상태바** — 진행 주차 / 보유 현금 / 금리 / 서울 LTV + 체력 게이지(35 미만이면 빨강).

**구성 요소 (위→아래)**
1. 시장 변수 카드 (해당 주에 발생했을 때만)
2. 매물 슬립 — 지역 배너(heat에 따라 색·문구 변화), 단지명, 호가(변동 시 원래 호가와 증감률 병기), 스펙 4칸, **실거래 근거 줄(실거래 모드 전용)**, 임장 기록(장점/단점), 자금 계산표
3. 현장 이벤트 카드 (발생 시)
4. 액션 버튼 5종
5. 관심 매물 목록 (있을 때)
6. 임장 동선 — 몇 주차에 어느 지역·단지를 보고 어떤 행동을 했는지 시간순(최신순)으로 정리한 목록. 그냥 넘긴 매물도 빠짐없이 남는다
7. 임장 일지

**실거래 근거 줄** (실거래 모드 전용, `L.real`이 있을 때만)
`최근 N건 실거래 중앙값 X · 최종 거래 YYYY년 M월 · K-apt 단지정보 반영 · *총층은 관측 최대층 기준 추정`
총층이 추정치(`totalFloorsEst`)면 스펙 칸의 층 표기에도 `*`가 붙는다.

**액션**

| 버튼 | 효과 | 체력 |
|---|---|---|
| 가계약금 넣기 | 계약 시도. 자금 부족 시 비활성 | −10 |
| 가격 협상하기 | 성공 시 감액, 실패 시 30% 확률로 매물 회수 | −12 |
| 관심 매물에 담고 다음 주로 | `watch`에 추가 후 진행 | (임장 −8) |
| 그냥 넘기기 | 다음 주로 | (임장 −8) |
| 이번 주는 쉬기 | 한 주 소모, 매물 없음 | +28 |
| 다시 보러 가기 (관심 매물) | 호가 재계산 후 `current`로 승격 | −6 |

**heat 배너 규칙**

| heat | 배너 색 | 문구 |
|---|---|---|
| > 0.6 | 빨강 | 문의 많음 · 경쟁 예상 |
| 0.3 ~ 0.6 | 파랑 | 일반 매물 |
| < 0.3 | 초록 | 장기 매물 · 협상 여지 |

**체력 35 미만일 때** — 임장 기록의 **단점 목록에 blur 처리**하고 헤더에 "피곤해서 놓친 부분이 있습니다"를 붙인다. 정보는 DOM에 존재하되 시각적으로 읽을 수 없다.

### 4.4 END — 결과

계약 성공 시:
1. **계약서 카드** — 이중선 제목, 매수인·소재지·면적·연식·계약 시점·매매대금. 우하단 빨간 원형 도장이 0.5초 후 1회 찍히는 애니메이션 (`prefers-reduced-motion`이면 생략).
2. **자금 조달** — 자기자본 / 대출 / 부대비용 / 잔여 현금 / 월 원리금. 월 상환이 월소득의 40%를 넘으면 경고 문구 추가.
3. **만족도** — 100점 만점 점수 + 총평 + 7개 항목 막대 그래프(선택한 우선순위에 ★).
4. **1년 뒤** — 실거래가 변동과 한 줄 코멘트.

미계약 시: 본 매물 수, 기간 중 시장 변동률, 최종 금리를 보여주는 무주택 엔딩.

마지막에 전체 임장 동선, 임장 일지와 "다시 시작하기".

---

## 5. 계산 로직

모든 금액 단위는 **만원**. 가격은 100만원 단위로 반올림한다.

### 5.1 통근 시간

```
dist    = hypot(region.x - office.x, region.y - office.y)
commute = round(12 + dist × 2.4 × region.t)   // 분, 편도
```

기저 12분은 도보·환승·대기 시간이다. 직장이 이벤트로 바뀌면 `current`와 `watch` 매물의 `commute`를 재계산한다.

### 5.2 매물 가격

**절차적 모드**

```
price = region.p × size.mul × size.py
      × ageFactor × floorFactor × flagFactor
      × marketIdx × U(0.95, 1.06)
```

| 구분 | 조건 | 계수 |
|---|---|---|
| ageFactor | ≤5년 | 1.16 |
| | ≤12년 | 1.03 |
| | ≤20년 | 0.90 |
| | ≤28년 | 0.80 |
| | 29년+ 재건축 | 0.92 |
| | 29년+ 그 외 | 0.72 |
| floorFactor | 1~2층 | 0.92 |
| | 탑층 | 0.96 |
| | 상위 45% 구간 | 1.04 |
| | 그 외 | 1.00 |
| flagFactor | 역세권 | ×1.06 |
| | 대단지 | ×1.04 |
| | 초품아 | ×1.03 |
| | 소음 | ×0.95 |
| | 언덕 | ×0.96 |

**실거래 모드 (호가 환산)**

```
ask   = median × askPremium            // 로드 시 1회, loadPool에서는 아니고 후보 생성 시 매번 재계산
price = ask × floorFactor × marketIdx × U(0.97, 1.03)
```

중앙값은 이미 연식·입지·단지 프리미엄을 전부 포함하므로 `ageFactor`와 `flagFactor`를 곱하지 않는다. 중복 반영하면 구축이 두 번 깎인다. `floorFactor`만 남기는 건 중앙값이 여러 층의 혼합이라 개별 층으로 되돌려야 하기 때문이다.

`askPremium` 기본 1.05(`listings.json`의 `meta.askPremium`). 상승장 1.08, 하락장 1.02 근처가 현실적이다.

`basePrice`에 최초 산정값을 보관하고, 이후 이벤트로 변동한 `price`와 비교해 증감률을 표시한다. 이건 두 모드 공통이다.

### 5.2.1 예산 재시도 (매물은 항상 살 수 있는 범위 안에서만 나온다)

`makeListing`은 지역·평형·연식·층·플래그·가격을 한 번에 뽑는 후보 생성 함수(절차적: `buildListingCandidate`, 실거래: `buildPoolCandidate`)를 반복 호출해, `canAfford(price, zone, py)`를 만족하는 첫 후보를 채택한다. 두 모드가 이 재시도 루프를 공유하므로 실거래 모드도 예외 없이 예산 안에서만 매물이 나온다.

```
후보 = build()   // POOL 있으면 buildPoolCandidate, 없으면 buildListingCandidate
최대 29번 재시도: canAfford(후보.price, 후보.r.z, 후보.size.py) 될 때까지
여전히 실패하면: 후보.price = min(후보.price, maxPrice(후보.r.z, 후보.size.py))  // 극단적 저예산 안전장치
```

- 지역 후보 풀은 예산으로 사전 제외하지 않는다(§5.6) — 재시도 과정에서 비싼 지역이 자연스럽게 걸러진다.
- 30번 시도해도 예산에 맞는 조합이 없을 만큼 극단적으로 예산이 낮을 때만, 마지막 후보의 가격을 그 지역·평형에서 낼 수 있는 최대가로 강제로 낮춘다. 실전에서는 거의 발생하지 않는다.
- 이 로직 덕분에 임장 중 뜨는 매물은 **항상 사용자가 설정한 연소득·현금 기준 대출 한도 안에서 실제로 계약 가능한 가격**으로만 나온다. 이전 버전에 있던, 예산을 넘는 "동기 부여용" 매물 노출은 제거했다.
- 단, 관심 매물을 재방문(§5.11)할 때는 시간 경과에 따른 시세 drift로 가격이 올라 예산을 넘어설 수 있다 — 이건 새 매물이 아니라 "기다리는 동안 놓친" 결과이므로 이 재시도 로직을 적용하지 않는다.

### 5.3 대출 한도

30년 원리금균등 기준, 원금 1만원당 연 상환액:

```
r = rate / 100 / 12
annPerMan(rate) = 12 × r / (1 − (1+r)^(−360))
// rate=4.2% → 0.05868 (1억당 연 586.8만원)
```

```
ltv    = firstHome ? 80 : (zone === "서울" ? ltvSeoul : ltvGyeonggi)
byLtv  = price × ltv / 100
byDsr  = max(0, income × 0.40 − debtAnnual) / annPerMan(rate + DSR_STRESS)
loan   = max(0, min(byLtv, byDsr, loanHardCap))
```

- `firstHome`(생애최초)은 지역·가격에 관계없이 LTV 80%를 단일 적용한다 (2023년 "생애최초 LTV 80% 단일화" 조치 기준. 지역별 기본 LTV에 가산하는 방식이 아니다).
- `DSR_STRESS = 1.5`(%p)는 2025년 7월 시행된 스트레스 DSR 3단계 가산금리를 단순화해 반영한 값이다. DSR 한도 계산에만 쓰이고, 실제 월 상환액(`annPerMan(rate)`)에는 가산하지 않는다.
- 세 가지 제약(LTV·스트레스 DSR 40%·절대 한도 6억) 중 가장 작은 값이 실제 한도다. `loanHardCap` 6억원은 2025년 "6.27 대책"으로 도입된 수도권 전역 주담대 한도를 반영한다.

### 5.4 부대비용

```
취득세    = price ≤ 60000 ? ×1.1% : price ≤ 90000 ? ×2.2% : ×3.3%
중개보수  = price < 20000 ? 0.5% : < 90000 ? 0.4% : < 120000 ? 0.5% : < 150000 ? 0.6% : 0.7%
기타      = 900 + size.py × 22        // 법무·이사·기본수리
sideCost  = 취득세 + 중개보수 + 기타
```

### 5.5 매수 가능 여부 / 최대 예산

```
canAfford(price) = cash ≥ price − loan(price) + sideCost(price)
```

`loan`이 `price`에 의존하므로 최대가는 닫힌 해가 없다. **0~60억 구간을 40회 이분 탐색**해 구한다. SETUP 미리보기와 매물 생성 시 예산 상한으로 사용한다.

### 5.6 지역 선호도 (regionFit)

```
commuteScore = clamp(5 − (commute − 22) / 12, 1, 5)
w(k)         = prio.includes(k) ? 2.6 : 1
regionFit    = commuteScore×w("commute") + edu×w("edu")
             + fut×w("fut") + env×w("env") + inf×w("inf")
```

매물 생성 시 `activeRegions()`(= `regionUniverse()`에서 `S.regions`로 고른 지역들, 비어 있으면 전체 폴백)를 `round(regionFit)` 만큼 후보 풀에 중복 투입해 가중 추첨한다(지역을 예산으로 사전에 걸러내지는 않는다 — 저예산 지역도 선택했다면 목록에는 있지만 가중치가 낮아 덜 뽑힐 뿐이다). 최종적으로 나오는 매물은 §5.2.1의 예산 재시도 로직을 반드시 통과하므로, 실제 사용자 예산을 넘는 매물은 뜨지 않는다. 인트로 몽타주(§4.2)의 방문 지점과 지도 위 점 표시도 `activeRegions()`를 쓴다.

`regionUniverse()`는 `POOL`이 있으면 `Object.values(POOL.regions)`(로드한 `dongMeta`), 없으면 상수 `REGIONS` 36건을 돌려준다(§3.7). 즉 실거래 모드에서는 "전체"의 의미가 그 데이터가 담고 있는 법정동 전체로 좁혀진다.

### 5.7 heat (경쟁 과열도)

```
heat = clamp( regionFit/28 × 0.6
            + (age ≤ 7 ? 0.25 : 0)
            + (station ? 0.12 : 0)
            + U(−0.10, 0.15)
            + (marketMood − 1) × 1.2,
            0.05, 0.95 )
```

### 5.8 계약 / 협상 성공률

```
계약 = clamp(0.86 − heat×0.55 + (stamina/100)×0.08 − (marketMood−1)×0.25, 0.12, 0.94)
협상 = clamp(0.55 − heat×0.45 + (stamina/100)×0.12 − (marketMood−1)×0.30, 0.05, 0.85)
```

- 계약 실패 → "매물 소진". 해당 매물은 `watch`에서도 제거된다.
- 협상 성공 → 가격 `−U(1.2%, 4.2%)`.
- 협상 실패 → 30% 확률로 매물 회수(`current = null`, `watch`에서 제거), 나머지는 가격 유지.
- 자금 부족 상태에서 계약 시도 시 성공률 판정 없이 부족액 안내 후 종료(체력만 −10).

### 5.9 만족도 (0~100)

항목별 1~5 점수:

| 항목 | 산식 |
|---|---|
| commute | `clamp(5 − (commute−22)/12, 1, 5)` |
| edu / fut / inf | `region.edu` / `region.fut` / `region.inf` |
| fresh | `clamp(5 − age/7, 1, 5)` |
| size | `clamp((py − 22)/5.5, 1, 5)` |
| env | `region.env − (noise ? 1 : 0) − (hill ? 0.5 : 0)` |
| parking | `clamp((parking − 0.5)/1.3 × 4 + 1, 1, 5)` |
| big | `clamp(units/2400 × 4 + 1, 1, 5)` |
| value | `clamp(5 − (price / maxPrice(zone, py)) × 4, 1, 5)` — 그 지역·평형에서 낼 수 있는 최대가 대비 저렴할수록 높다 |

```
w(k)  = prio.includes(k) ? 3 : 1
base  = Σ(score×w) / Σ(5×w) × 100
base −= max(0, 월상환 − 월소득×0.35) × 0.8    // 상환 부담 감점
base += renov ? 3 : 0
base −= leak  ? 4 : 0
result = clamp(round(base), 5, 99)
```

> 실거래 모드는 `size.py`가 전용면적 기준이라 같은 집이 절차적 모드(공급면적 기준)보다 `size` 점수를 낮게 받는다. 우선순위에 "넓은 집"을 넣었을 때 체감 차이가 크다. 모드별 보정이 필요하면 `(py − 22)/5.5`의 기준점 22를 전용 18 정도로 내린다.

### 5.10 1년 후 시세

```
growth(%) = (marketMood − 1)×9 + (region.fut − 3)×2.2
          + (recon ? 3 : 0) + U(−4, 5) − (age > 30 ? 1 : 0)
```

구간별 코멘트: `>7%` / `>0%` / `>−5%` / 그 이하 4단계.

### 5.11 관심 매물 재방문

```
weeks = 현재주차 − seenWeek
drift = (marketMood − 1) × 0.02 × weeks + U(−0.01, 0.03) × weeks
소진확률 = clamp(0.1 + heat×0.3, 0.1, 0.5) × weeks × 0.35
```

소진 판정을 먼저 하고, 살아남았으면 `price`에 `drift`를 적용한 뒤 `current`로 올린다. `commute`도 재계산한다(직장 이전 반영).

---

## 6. 이벤트 명세

### 6.1 현장 이벤트 — 매물 제시 시 **42%** 확률, 가중 추첨 (총 가중치 58)

| 가중치 | 이름 | 효과 |
|---|---|---|
| 10 | 호가 인상 | `price += 1500~6000` |
| 8 | 누수 이력 발견 | `leak = true`, `price −= 800~2500` |
| 7 | 세입자 비협조 | `tenant = true` |
| 6 | 급매 전환 | `price −= 2000~7000`, `heat −= 0.2` |
| 6 | 경쟁자 등장 | `heat += 0.25` |
| 5 | 층간소음 | `noise = true` |
| 5 | 수리 상태 양호 | `renov = true` |
| 4 | 재건축 호재 | 연식 25년 미만이면 **스킵**. 아니면 `recon = true`, `price += 3000~9000` |
| 4 | 조망 발견 | `south = true` |
| 3 | 중개사 제안 | `price −= 500~1500` |

이벤트 함수가 `null`을 반환하면(조건 미충족) 해당 주는 이벤트 없이 진행한다.

### 6.2 시장 이벤트 — 매주 **30%** 확률, 가중 추첨 (총 가중치 69)

| 가중치 | 이름 | 효과 |
|---|---|---|
| 9 | 금리 인상 | `rate += 0.25~0.6` (상한 8%) |
| 8 | 금리 인하 | `rate −= 0.25~0.6` (하한 2.5%) |
| 8 | 시장 과열 | `marketIdx ×= 1.028`, `marketMood += 0.12` |
| 8 | 관망세 | `marketIdx ×= 0.982`, `marketMood −= 0.12` |
| 7 | 대출 규제 강화 | `ltvSeoul −= 10` (하한 30), `loanHardCap −= 10000` (하한 3억) |
| 5 | 규제 완화 | `ltvSeoul += 10` (상한 70), `loanHardCap += 10000` |
| 5 | 가족 지원 | `cash += 2000~8000` |
| 4 | 성과급 | `cash += 400~1800` |
| 4 | 근무지 변경 | `office` 를 다른 곳으로 변경 |
| 4 | 예상 못한 지출 | `cash −= 300~1500` (하한 0) |
| 4 | 토지거래허가구역 | `ltvSeoul −= 5` |
| 3 | 이직 제안 | `income += 400~1200` |

`marketMood`는 0.6~1.5로 clamp한다.

### 6.3 체력 규칙

| 행동 | 변화 |
|---|---|
| 매주 임장 | −8 |
| 계약 시도 | −10 |
| 협상 | −12 |
| 재방문 | −6 |
| 휴식 (1주 소모) | +28 |

35 미만이면 단점 리포트 blur, 계약·협상 성공률에 최대 −0.08 / −0.12 반영.

---

## 7. 디자인 규약

| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#16302F` | 페이지 바닥 (짙은 청록) |
| `--paper` | `#FFFDF6` | 종이 카드 |
| `--ink` | `#171A18` | 본문·테두리 |
| `--red` | `#C8202B` | 상승·경쟁·도장 |
| `--blue` | `#134A9A` | 하락·일반 배너 |
| `--hl` | `#FFD93B` | 이벤트 카드 |
| `--ok` | `#1E7A4C` | 장점·비경쟁 매물 |

- 상승 = 빨강, 하락 = 파랑 (국내 관행). 반대로 쓰지 않는다.
- 제목·금액은 `Black Han Sans`, 본문은 `IBM Plex Sans KR`. 숫자는 전부 `tabular-nums`.
- 카드는 2px 검정 테두리 + `5px 5px 0` 오프셋 그림자. 부동산 창문에 붙은 전단 인쇄물이 레퍼런스다. 그라디언트·둥근 모서리·소프트 섀도는 쓰지 않는다.
- 움직이는 요소는 **최종 계약서 도장**과 **시작 몽타주의 지도 마커·궤적**, 둘뿐이다.
- 인트로 마커는 외부 이미지가 아니라 **인라인 SVG**로 직접 그린다(§4.2). 저장소 루트의 `imjang-character-28x33.png`는 배색·형태를 잡을 때 참고한 레퍼런스 스프라이트일 뿐, 게임이 실제로 로드해 쓰지는 않는다.

---

## 8. 비기능 요구사항

| 항목 | 요구 |
|---|---|
| 반응형 | 375px 폭까지 가로 스크롤 없이 동작 |
| 접근성 | 모든 인터랙션은 버튼 요소. `aria-pressed`로 칩 상태 노출. 포커스 링 3px 노랑 |
| 모션 | `prefers-reduced-motion: reduce` 시 도장 애니메이션·버튼 트랜지션·시작 몽타주 제거(몽타주는 생략하고 바로 TOUR 1주차로 진입) |
| 저장 | 없음. 새로고침 = 초기화. `POOL`만 예외(게임 재시작에는 유지) |
| 네트워크 | 폰트 외 요청 없음. 실거래 데이터는 파일로 주입하며, 오프라인에서도 플레이 가능 |
| 브라우저 | 모던 에버그린 브라우저. `Math.hypot`, 옵셔널 체이닝, 스프레드, `FileReader` 사용 |
| 데이터 수집 | Python 3 표준 라이브러리만. 외부 패키지 없음 (`pilot/`) |

**게임이 API를 직접 부르지 않는 이유** — data.go.kr는 CORS를 열지 않고, 프런트에서 부르면 서비스키가 노출된다. 실거래 신고 기한이 30일이라 어차피 실시간이 성립하지 않는다.

---

## 9. 알려진 제약

1. **시세가 고정 상수다.** 실제 시장과 시점이 어긋나면 `REGIONS[].p`를 통째로 갱신해야 한다.
2. **규제 수치가 하드코딩이다.** LTV·DSR 40%·6억 한도·스트레스 DSR 가산금리(1.5%p)는 2025년 하반기 기준을 단순화한 값이며, 규제지역별 세부 차등(강남3구·용산 등 토지거래허가구역 단위)·특례보금자리론·신생아특례·디딤돌대출 등 정책상품은 반영하지 않았다.
3. **DSR에 신용대출·마이너스통장이 없다.** `debtAnnual` 한 값으로 뭉갠다.
4. **매물은 매주 새로 생성된다.** 같은 단지의 다른 층·다른 평형이 다시 나오는 일이 없어, 특정 단지를 추적하는 플레이가 불가능하다. (실거래 모드도 동일 — 매번 `POOL.items`에서 새로 추첨한다.)
5. **평당가가 단지 특성을 반영하지 않는다.** 절차적 모드는 같은 지역이면 브랜드·단지 규모에 관계없이 동일 기준가에서 출발한다.
6. **`dongMeta`의 `edu`/`inf`/`env`/`fut`는 주관 점수다.** 데이터가 아니라 사람이 채운 값이고, 구를 추가할 때마다 손이 간다 (`pilot/collect_listings.py`의 `DONG_META`).
7. **총층은 추정치다.** K-apt에 최고층 정보가 없어 실거래 관측 최대층 + 2를 쓴다(`totalFloorsEst`). 정확히 하려면 건축물대장 표제부(`grndFlrCnt`)를 붙여야 한다.
8. **K-apt 매칭은 단지명 문자열 비교다.** 퍼지매칭 컷오프 0.82에서 오매칭이 나올 수 있다. `matchScore` 검수 전제.
9. **최근 1~2개월 표본이 얇다.** 실거래 신고 기한 때문에 아직 안 들어온 거래가 있다.
10. **실거래 모드는 한 번에 한 구만 다룬다.** 여러 구를 섞으려면 `listings.json`을 병합하는 단계가 따로 필요하다.
11. **동일 단지가 연속으로 나올 수 있다.** 추첨에 최근 등장 이력을 반영하지 않는다(위 4번과 같은 원인).

---

## 10. 향후 과제

| 우선도 | 항목 | 메모 |
|---|---|---|
| 높음 | 단지 풀(pool) 고정 (절차적 모드) | 지역별 단지 8~12개를 게임 시작 시 생성해두고 그 안에서 매물을 뽑는다. 같은 단지 재등장·시세 추적 가능해짐. **실거래 모드는 이미 고정된 실제 단지를 쓰지만, "같은 단지 재등장"은 아직 안 됨(§9-4)** |
| 높음 | 다중 구 병합 (실거래 모드) | `listings.json`을 배열로 받아 합치고, `zone`이 섞이면 LTV가 매물별로 갈리도록 |
| 높음 | 주차별 시세 그래프 | 관심 단지의 호가 추이를 라인 차트로. 재방문 판단 근거가 생긴다 |
| 중간 | 최근 등장 쿨다운 | 같은 단지가 3주 내 재등장하지 않도록 추첨 보정 (§9-11) |
| 중간 | 건축물대장 총층 | 표제부 `grndFlrCnt`로 `totalFloorsEst` 추정치 제거 |
| 중간 | 전세 끼고 매수(갭) | `tenant` 플래그를 실제 보증금 승계로 연결. 현금 부담이 크게 달라짐 |
| 중간 | 배우자 의사 | 가구 형태가 신혼·자녀일 때 계약 전 동의 판정. 우선순위가 두 벌이 된다 |
| 중간 | 중개사 신뢰도 | 같은 중개사를 반복 방문하면 급매 정보 우선 노출 |
| 낮음 | 난이도 프리셋 | 상승장 / 하락장 / 횡보장 시작 옵션 |
| 낮음 | 결과 공유 | 계약서 카드를 이미지로 내보내기 |

---

## 11. 밸런싱 튜닝 포인트

수치를 건드릴 때 가장 먼저 볼 상수들이다.

| 상수 | 현재값 | 올리면 |
|---|---|---|
| 시장 이벤트 발생률 | 0.30 | 외부 변수에 휘둘리는 게임이 됨 |
| 현장 이벤트 발생률 | 0.42 | 매물 하나하나가 드라마틱해짐 |
| 계약 성공률 기저 | 0.86 | 쉽게 사고 게임이 짧아짐 |
| `heat` 계수 | 0.55 / 0.45 | 인기 지역이 사실상 봉쇄됨 |
| 주간 체력 소모 | 8 | 휴식 턴을 강제하게 됨 |
| 예산 재시도 횟수(§5.2.1) | 30 | 낮추면 극단적 저예산에서 강제 최대가 매물이 더 자주 나옴 |
| 만족도 상환부담 감점 계수 | 0.8 | 무리한 대출의 페널티가 커짐 |
| `meta.askPremium` (실거래 모드) | 1.05 | 호가가 전반적으로 비싸져 계약이 어려워짐 |
| `pilot/collect_listings.py --min-deals` | 3 | 표본이 얇은 단지가 걸러져 후보가 줄어듦 |
